/**
 * Verifies that an integer is an odd number.
 *
 * @param n The number to verify.
 * @return `true` if odd; `false` otherwise.
 */
function isOdd(n) {
  return n % 2 !== 0;
}

export { isOdd };

