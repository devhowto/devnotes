/**
 * Verifies that an integer is an even number.
 *
 * @param n The number to verify.
 * @return `true` if even; `false` otherwise.
 */
function isEven(n) {
  return n % 2 === 0;
}

export { isEven };
